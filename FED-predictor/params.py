# Global project params

import os

# ENCODER.PY
RENAME_RATES = {'Release Date': 'date', 'Actual': 'rate'}
##FINAL_COLUMNS = ['date', 'decision', 'grouped_statements', 'grouped_minutes', 'decision_encoded']